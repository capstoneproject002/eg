%tr
  %td= attachment.id
  %td= attachment.user.name
  %td= attachment.title
  %td= attachment.class_room.name
  %td.action
    = link_to(attachment, class: 'btn btn-success btn-xs', toogle: 'tooltip', title: t('tooltips.zoom')) do
      Detay
    = link_to(edit_attachment_path(attachment) , class: 'btn btn-info btn-xs') do
      Düzenle
    = link_to(attachment, class: 'btn btn-danger btn-xs', method: :delete) do
      Sil